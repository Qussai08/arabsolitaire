name: game_solver
description: Pure Dart solver for Solitaire Al-Arab (depends on game_engine).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.1

dependencies:
  game_engine:
    path: ../game_engine

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
